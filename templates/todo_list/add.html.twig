{% extends 'base.html.twig' %}

{% block title %}Hello World!{% endblock %}

{% block body %}
    Add your todos
    {{ form_start(form, {'action': path('new'), 'method': 'POST'}) }}
        {{ form_errors(form) }}
        {{ form(form) }}
    {{ form_end(form) }}
{% endblock %}